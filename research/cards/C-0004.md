# C-0004 · effect beats text on indirect destruction · track: rules → model · method: M-effect-input · status: queued (sub-project 2 start)
Observation: Text guards are bypassed by indirection: GuardFall got through 10 of 11 agents, #85274 lists script, interpreter, find and xargs bypasses, and in SafeClawBench 291 of 347 harms passed text checks. This is from the literature and not yet reproduced on our data. Counter-evidence: 2509.11686 found execution traces add little.
Hypothesis: Judging the observed effect removes the indirection that defeats text judges, so the gain concentrates on the indirect class.
Change: The same judge given (a) command text only, (b) EffectRecord only, (c) both. First the rules version (regex on text vs rules on effects), then a model version after C-0005.
Prediction: On the indirect-destruction class, miss@ask5 for (b) and (c) is well below (a), beyond the CI. On the direct class the difference is within noise.
Disproof: (b) is not better than (a) on the indirect class beyond the CI. The core claim then fails, and this is reported as a negative result.
Budget: rules 3 × 2 min; model 3 arms × 3 seeds × 20 min.
Verdict:
