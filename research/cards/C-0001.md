# C-0001 · overlay per-file metadata cost · track: sandbox-perf · method: M-overhead-diagnosis · status: queued (sub-project 1)
Observation: The worst-case write-heavy workload runs at 1.65× native inside the userns overlay (measured 2026-09-22), above the 1.5× target.
Hypothesis: Per-file overlay work (copy-up, whiteouts, xattrs) dominates, so overhead grows with the number of files touched.
Change: 2×2 microbenchmark: {200, 20k} files × {10 MB, 1 GB} total bytes, native vs shadow; shared with C-0002.
Prediction: Overhead ratio rises with file count at fixed bytes; it barely moves with bytes at a fixed file count.
Disproof: The ratio is flat across file count (within 0.05×).
Budget: 20 min CPU (one run shared with C-0002 and C-0003).
Verdict: not yet run. The first attempt (2026-09-23) was stopped by the host under memory pressure before it produced any rows. Re-run `scripts/dev/bench.sh overlay_factorial` on an idle machine.
