# C-0005 · reference arm: plain SFT + tuned threshold · track: model · method: M-sft-threshold · status: queued (sub-project 3 start)
Observation: No learned judge exists yet. Every later objective (C-0006, C-0007) needs a reference taken at its own tuned threshold.
Hypothesis: A ≤ 1.7B judge fine-tuned with plain SFT, temperature-scaled, and thresholded at benign-ask ≤ 5% already handles the asymmetric error costs at decision time.
Change: QLoRA SFT on dev-iterate training data; decision read from label-token probabilities in one forward pass; temperature scaling; threshold fitted on the calibration fold.
Prediction: ECE ≤ 0.05 after scaling; p95 latency ≤ 100 ms; miss@ask5 below the best rules-only arm beyond the CI.
Disproof: Latency > 100 ms p95 (change base model or input format), or no gain over rules-only beyond the CI (the effect record is then doing the work, which is reportable).
Budget: 3 seeds × 20 min GPU.
Verdict:
