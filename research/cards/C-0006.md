# C-0006 · cost-weighted cross-entropy · track: model · method: M-weighted-ce · status: blocked on C-0005
Observation: To be filled from C-0005: where the reference arm's misses sit relative to the threshold, and its pAUC over benign-ask ∈ [0, 5%].
Hypothesis: The current objective or decision threshold may not reflect the asymmetric error costs. Reweighting destructive examples in training may improve *ranking* near the operating point, beyond what moving the threshold already gives.
Change: Weight destructive items by w ∈ {3, 10} in the SFT loss; re-tune each arm's own threshold.
Prediction: pAUC[0, 5%] rises and miss@ask5 falls beyond the CI relative to C-0005, at each arm's tuned threshold.
Disproof: No gain beyond the CI once thresholds are re-tuned. The benefit was then only an operating-point shift, which threshold tuning already provides. Abandon.
Budget: 2 weights × 3 seeds × 20 min GPU; cost logged.
Verdict:
