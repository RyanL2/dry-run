# C-0008 · sandbox-rewarded attacker vs random obfuscation · track: data · method: M-attacker-lora · status: blocked on sub-project 2 generator
Observation: To be filled from the first judge evaluations: how many dev-gen misses come from obfuscation families that no training generator covers.
Hypothesis: An attacker LoRA rewarded only when the sandbox observes destruction the judge allowed finds judge blind spots faster than random composition of obfuscation operators.
Change: One offline round with equal GPU budget: attacker LoRA vs random-operator generator; both outputs verified in disposable containers.
Prediction: More unique sandbox-verified misses per GPU-hour, and retraining on them lowers dev-gen miss@ask5 beyond the CI.
Disproof: The random generator yields ≥ as many verified misses per GPU-hour. Abandon the attacker.
Budget: 2 × 60 min GPU.
Verdict:
